{
  "name": "agent-creator-workspace",
  "version": "0.4.2",
  "private": true,
  "description": "Workspace for the Agent Creator CLI and composable Agent runtime.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:plain": "npm run build --workspace @agent-creator/core && npm run build --workspace agent-creator-cli",
    "dev": "npm run dev --workspace agent-creator-cli",
    "test": "npm run test --workspace @agent-creator/core && npm run test --workspace agent-creator-cli && vitest run tests/versionScripts.test.ts",
    "validate": "npm run validate --workspace agent-creator-cli",
    "commit": "node scripts/commit.mjs",
    "check:package": "node scripts/check-package-contents.mjs",
    "publish": "node scripts/publish.mjs --tag latest",
    "publish:beta": "node scripts/publish.mjs --tag beta",
    "prepare": "husky || true"
  },
  "devDependencies": {
    "@types/node": "^20.14.12",
    "husky": "^9.1.7",
    "tsx": "^4.16.2",
    "vitest": "^2.0.5"
  }
}
